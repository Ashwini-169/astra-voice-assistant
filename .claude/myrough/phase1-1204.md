Phase-1 diagnostics complete (issues only, no fixes applied).

1. High: full `pytest` run is broken at collection level.
- Command: `.\venv\python.exe -m pytest -q`
- Failures include 10 `PermissionError [WinError 5]` on root-level `pytest-cache-files-*` directories and one collection-time exception from `test_audio.py`.
- Problematic script executes HTTP at import time in [test_audio.py](I:\Project\voice2\test_audio.py:6) and forces JSON parsing at [test_audio.py](I:\Project\voice2\test_audio.py:13), which crashed when endpoint returned non-JSON (`Status: 500`).

2. High: interactive test blocks CI/non-interactive pytest execution.
- Command: `.\venv\python.exe -m pytest tests -q`
- Result: `1 failed, 54 passed`.
- Failing test reads stdin during pytest capture at [test_speech_recognition_interactive.py](I:\Project\voice2\tests\test_speech_recognition_interactive.py:74) and again at [test_speech_recognition_interactive.py](I:\Project\voice2\tests\test_speech_recognition_interactive.py:185), raising `OSError: pytest: reading from stdin while output is captured`.

3. High: runtime app path has service/model availability instability.
- Command: `.\venv\python.exe orchestrator\main.py --text "hello"`
- Observed output includes `assistant_text: "Service error: "` and pipeline failure logs.
- Same run repeatedly failed to fetch HF model assets due proxy connection refusal to `127.0.0.1:9`, then timed out.

4. Medium: graph MCP advanced diagnostics are timing out.
- `get_minimal_context`, `detect_changes`, `get_affected_flows`, `get_impact_radius` each timed out at 120s in this session.
- Lightweight graph calls (`list_graph_stats`, `list_flows`, `list_communities`) worked.

5. Medium: coverage gaps on critical orchestrator entrypoints (graph evidence).
- `tests_for` returned 0 for [orchestrator main](I:\Project\voice2\orchestrator\main.py:384) (`main`).
- `tests_for` returned 0 for [orchestrator pipeline](I:\Project\voice2\orchestrator\pipeline.py:119) (`run_pipeline`) and [streaming pipeline](I:\Project\voice2\orchestrator\pipeline.py:204) (`run_pipeline_streaming`).

6. Low: `.claude` skill markdown has encoding corruption.
- In [debug-issue.md](I:\Project\voice2\.claude\skills\debug-issue.md:27), `≤` appears as mojibake (`â‰¤`), indicating file encoding inconsistency.

If you want, next phase I’ll start applying fixes in priority order.


Important Insight (from your file)

Your file literally explains same concept:

👉 Persistent + streaming pipelines feel faster
👉 Loading + blocking kills performance

⚠️ Why your system behaves like this

Most likely:

❌ You're using:
ollama generate without stream
or HTTP POST that waits full JSON

Example (blocking):

response = requests.post("/api/generate", json=payload)
print(response.json())
✅ How WebUI does it

They use stream=true

Example:

curl http://localhost:11434/api/generate -d '{
  "model": "qwen",
  "prompt": "Hello",
  "stream": true
}'

Response:

{ "response": "Hel", "done": false }
{ "response": "Hello", "done": false }
{ "response": "Hello world", "done": true }
🚀 How YOU should fix (VERY IMPORTANT)
✅ Option 1: Enable streaming in backend

Python example:

import requests

response = requests.post(
    "http://localhost:11434/api/generate",
    json={
        "model": "qwen",
        "prompt": "Hello",
        "stream": True
    },
    stream=True
)

for line in response.iter_lines():
    if line:
        print(line.decode())
✅ Option 2: FastAPI streaming (BEST for your project)

Since you're building Django + React + PyQt:

👉 Use StreamingResponse

from fastapi.responses import StreamingResponse

async def stream_llm():
    async for chunk in ollama_stream():
        yield chunk

return StreamingResponse(stream_llm(), media_type="text/plain")
✅ Option 3: Frontend streaming (React)

Instead of waiting:

fetch("/api")

Use:

const reader = response.body.getReader()
🧠 Bonus: Why first word delay still ~7 sec

Even in WebUI you noticed:

first word comes after ~7 sec

That is:

🔹 Model warmup / first token latency

Caused by:

Model loading
KV cache init
GPU ramp-up

👉 Your file also explains this:

First request slow
Keep model loaded = faster next responses
⚡ Final Architecture You SHOULD Aim For
Mic → ASR → LLM (streaming) → TTS (chunked)

NOT:

Mic → ASR → LLM (wait full) → TTS